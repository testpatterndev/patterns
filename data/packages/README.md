# Curated sealed packages

Exact classifier membership for Compl8 / TestPattern deployment packages.

| Id | File | Custom SITs | Packs |
|---|---|---:|---:|
| qld-government-full | `qld-government-full.json` | 399 | 8 |

Each pattern in the sealed set is also tagged in `package-tags.json` with addon `qld-government-full` (recompiled into `patterns.json`).

Regenerate after reseal from Compl8DLPDeploy selection:
`config/package-secrets/qld-government-full-selection-from-399.json`
