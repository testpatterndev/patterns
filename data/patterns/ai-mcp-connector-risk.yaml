schema: testpattern/v1
name: AI MCP / Connector Risk
slug: ai-mcp-connector-risk
version: 0.1.0
type: keyword_list
description: >-
  Detects risky MCP / plugin / connector descriptors: tool manifests requesting over-broad or unexplained
  scopes, or encouraging autonomous action that bypasses user approval (OWASP LLM06 / Agentic Top 10).
  Phrase detection is corroborated by AI-context markers.
operation: >-
  One phrase regex (Pattern_mcp_broad_scope) matching scope/permission language adjacent to wildcard/full/admin
  grants, or approval-bypass framings, corroborated by AI-context markers. Single low tier (65 phrase only);
  developer prototypes, sandbox tools and public sample manifests are the named FP traps.
pattern: '(?i)(?:\b(?:scopes?|permissions?|access)\b[^.\n]{0,30}\b(?:\*|all|full|read[- ]?write everything|admin)\b|\bwithout (?:user )?(?:approval|confirmation|consent)\b)'
confidence: low
confidence_justification: >-
  Low by design. Broad-scope and approval-bypass language is common and benign in developer prototypes,
  sandbox tools and public sample manifests (the named FP traps). The hybrid approach pairs this seed with
  manifest/scope analysis; the keyword phrase alone is necessary-not-sufficient.
jurisdictions: [global]
regulations: ['OWASP LLM Top 10 2025', 'NIST AI RMF GenAI Profile']
frameworks: [ISO 27001]
data_categories: [emerging, security]
ai_threat:
  ai_sit_id: AI-SIT-009
  threat_family: AgentAction
  detection_approach: hybrid
corroborative_evidence:
  keywords: [connector, plugin, manifest, tool]
  proximity: 300
  keyword_lists: [ai-context-markers]
purview:
  patterns_proximity: 300
  recommended_confidence: 65
  regexes:
    - id: Pattern_mcp_broad_scope
      pattern: '(?i)(?:\b(?:scopes?|permissions?|access)\b[^.\n]{0,30}\b(?:\*|all|full|read[- ]?write everything|admin)\b|\bwithout (?:user )?(?:approval|confirmation|consent)\b)'
  shared_keywords:
    - dict: ai-context-markers
      as: Evidence_ai_context
      match_style: word
  pattern_tiers:
    - confidence_level: 65
      id_match: Pattern_mcp_broad_scope
risk_rating: 7
risk_description: >-
  An over-privileged or approval-bypassing connector lets an agent take high-impact autonomous actions
  outside the user's intent or oversight.
sensitivity_labels:
  us_gov: 'CUI Basic'
  uk_gov: 'OFFICIAL-SENSITIVE'
test_cases:
  should_match:
    - value: 'This MCP tool requests full admin access and runs actions without user approval.'
      description: Over-broad scope + approval bypass in a tool manifest
  should_not_match:
    - value: 'The connector requests read-only access to the calendar.'
      description: Narrow, scoped permission (FP trap)
created: '2026-06-08'
updated: '2026-06-08'
author: testpattern-community
license: MIT
keywords:
  - full access
  - full admin access
  - all scopes
  - all permissions
  - read-write everything
  - admin scope
  - without user approval
  - without confirmation
  - without consent
