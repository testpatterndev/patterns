schema: testpattern/v1
name: OAuth client secrets
slug: global-top500-267-oauth-client-secrets
version: 1.0.0
type: regex
engine: boost_regex
description: >-
  Identifies oauth client secrets patterns in security and access control contexts. Detects potential exposure of
  sensitive security information in international systems.
operation: >-
  Pattern type: regex (boost_regex engine). Regex uses structural anchors with proximity constraints to match
  domain-specific content. Corroborative evidence: 6 keywords within 300 characters. Keyword lists:
  classification-australia-government, credentials-tokens.
pattern: >-
  (?is)\b(secret|credential|token|password|certificate|private\s+key|oauth|session|mfa|vault|connection\s+string)\b.{0,180}\b(value|material|metadata|export|artifact|cookie)\b
confidence: medium
confidence_justification: category-aware structural regex with anchor and context constraints replaces phrase-only detection.
jurisdictions:
  - global
regulations:
  - general-data-protection
data_categories:
  - credentials
  - security
corroborative_evidence:
  keywords:
    - oauth client secrets
    - oauth
    - client
    - secrets
    - credentials
    - keys
  proximity: 300
  keyword_lists:
    - classification-australia-government
    - credentials-tokens
test_cases:
  should_match:
    - value: OAuth client secrets
      description: Exact phrase marker match
    - value: oauth client secrets
      description: Case-insensitive phrase match
    - value: OAuth client secrets
      description: Normalized whitespace phrase
    - value: structured artifact containing anchor terms and contextual fields
      description: Structural anchor sample
  should_not_match:
    - value: unrelated generic text
      description: No relevant phrase context
    - value: placeholder value 12345
      description: Random text should not match phrase marker
    - value: generic prose without structural artifact anchors
      description: Should not match plain mentions
false_positives:
  - description: >-
      Authentication-related terminology in software documentation, security training materials, or system architecture
      descriptions without actual credentials.
    mitigation: >-
      Require proximity to credential-specific patterns (API keys, connection strings, tokens) rather than general
      security terminology.
  - description: >-
      Code snippets and configuration examples containing credential-related keywords or placeholder values in developer
      documentation.
    mitigation: >-
      Check for common placeholder patterns (example.com, localhost, 0000) and documentation file types to reduce false
      positives from technical writing.
exports:
  - purview_xml
  - yaml
  - regex_copy
scope: wide
created: '2026-02-08'
updated: '2026-02-08'
author: testpattern-community
license: MIT
references: []
