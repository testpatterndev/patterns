schema: testpattern/v1
name: HSM key management metadata
slug: au-top500-274-hsm-key-management-metadata
version: 1.0.0
type: regex
engine: boost_regex
description: >-
  Identifies hsm key management metadata patterns in security and access control contexts. Detects potential exposure of
  sensitive security information in Australian systems.
operation: >-
  Pattern type: regex (boost_regex engine). Regex uses structural anchors with proximity constraints to match
  domain-specific content. Corroborative evidence: 8 keywords within 300 characters.
pattern: (?is)\b(?:hsm|pkcs#11|key\s+label|key\s+id|kms|key\s+slot)\b\s{0,200}\b(?:rotation|lifecycle|algorithm|policy)\b
confidence: medium
confidence_justification: structural regex with domain-specific anchors and constrained context replaces phrase-only marker.
jurisdictions:
  - au
regulations:
  - general-data-protection
data_categories:
  - credentials
  - security
corroborative_evidence:
  keywords:
    - hsm key management metadata
    - hsm
    - key
    - management
    - metadata
    - credentials
    - keys
    - secrets
  proximity: 300
test_cases:
  should_match:
    - value: HSM key management metadata
      description: Exact phrase marker match
    - value: hsm key management metadata
      description: Case-insensitive phrase match
    - value: HSM key management metadata
      description: Normalized whitespace phrase
    - value: structured sample with matching anchors
      description: Structural anchor sample
  should_not_match:
    - value: unrelated generic text
      description: No relevant phrase context
    - value: placeholder value 12345
      description: Random text should not match phrase marker
    - value: generic policy prose without anchors
      description: Should reject generic mentions without structural anchor terms
false_positives:
  - description: >-
      Authentication-related terminology in software documentation, security training materials, or system architecture
      descriptions without actual credentials.
    mitigation: >-
      Require proximity to credential-specific patterns (API keys, connection strings, tokens) rather than general
      security terminology.
  - description: >-
      Code snippets and configuration examples containing credential-related keywords or placeholder values in developer
      documentation.
    mitigation: >-
      Check for common placeholder patterns (example.com, localhost, 0000) and documentation file types to reduce false
      positives from technical writing.
exports:
  - purview_xml
  - yaml
  - regex_copy
scope: wide
sensitivity_labels:
  us_gov: CUI Specified
  uk_gov: OFFICIAL-SENSITIVE
  nz_gov: RESTRICTED
  ca_gov: Protected B
purview:
  patterns_proximity: 300
  recommended_confidence: 85
  pattern_tiers:
    - confidence_level: 85
      id_match: Regex_hsm_key_management
      matches:
        - ref: Keyword_hsm_key_management
    - confidence_level: 75
      id_match: Regex_hsm_key_management
  regexes:
    - id: Regex_hsm_key_management
      pattern: (?is)\b(?:hsm|pkcs#11|key\s+label|key\s+id|kms|key\s+slot)\b\s{0,200}\b(?:rotation|lifecycle|algorithm|policy)\b
  keywords:
    - id: Keyword_hsm_key_management
      groups:
        - match_style: word
          terms:
            - hsm key management
            - hardware security module
            - key rotation
            - pkcs#11
            - key lifecycle
            - cryptographic key
            - key custodian
            - key ceremony
            - key escrow
            - master key
            - kms
created: '2026-02-08'
updated: '2026-02-08'
author: testpattern-community
license: MIT
references: []
