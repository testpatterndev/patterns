schema: testpattern/v1
name: One-time recovery codes
slug: global-top500-265-one-time-recovery-codes
version: 1.0.0
type: regex
engine: boost_regex
description: >-
  Identifies one-time recovery codes patterns in security and access control contexts. Detects potential exposure of
  sensitive security information in international systems.
operation: >-
  Pattern type: regex (boost_regex engine). Regex uses structural anchors with proximity constraints to match
  domain-specific content. Corroborative evidence: 8 keywords within 300 characters.
pattern: >-
  (?is)\b(?:secret|credential|token|password|certificate|private\s+key|oauth|session|mfa|vault|connection\s+string)\b\s{0,180}\b(?:value|material|metadata|export|artifact|cookie)\b
confidence: medium
confidence_justification: category-aware structural regex with anchor and context constraints replaces phrase-only detection.
jurisdictions:
  - global
regulations:
  - general-data-protection
data_categories:
  - credentials
  - security
corroborative_evidence:
  keywords:
    - one-time recovery codes
    - one
    - time
    - recovery
    - codes
    - credentials
    - keys
    - secrets
  proximity: 300
test_cases:
  should_match:
    - value: One-time recovery codes
      description: Exact phrase marker match
    - value: one-time recovery codes
      description: Case-insensitive phrase match
    - value: One-time recovery codes
      description: Normalized whitespace phrase
    - value: structured artifact containing anchor terms and contextual fields
      description: Structural anchor sample
  should_not_match:
    - value: unrelated generic text
      description: No relevant phrase context
    - value: placeholder value 12345
      description: Random text should not match phrase marker
    - value: generic prose without structural artifact anchors
      description: Should not match plain mentions
false_positives:
  - description: >-
      Authentication-related terminology in software documentation, security training materials, or system architecture
      descriptions without actual credentials.
    mitigation: >-
      Require proximity to credential-specific patterns (API keys, connection strings, tokens) rather than general
      security terminology.
  - description: >-
      Code snippets and configuration examples containing credential-related keywords or placeholder values in developer
      documentation.
    mitigation: >-
      Check for common placeholder patterns (example.com, localhost, 0000) and documentation file types to reduce false
      positives from technical writing.
exports:
  - purview_xml
  - yaml
  - regex_copy
scope: wide
purview:
  patterns_proximity: 300
  recommended_confidence: 85
  pattern_tiers:
    - confidence_level: 85
      id_match: Regex_one_time_recovery_codes
      matches:
        - ref: Keyword_one_time_recovery_codes
    - confidence_level: 75
      id_match: Regex_one_time_recovery_codes
  regexes:
    - id: Regex_one_time_recovery_codes
      pattern: >-
        (?is)\b(?:secret|credential|token|password|certificate|private\s+key|oauth|session|mfa|vault|connection\s+string)\b\s{0,180}\b(?:value|material|metadata|export|artifact|cookie)\b
  keywords:
    - id: Keyword_one_time_recovery_codes
      groups:
        - match_style: word
          terms:
            - recovery code
            - one-time code
            - backup code
            - emergency code
            - otp
            - single-use
            - recovery token
            - account recovery
            - mfa recovery
            - bypass code
            - scratch code
            - reset code
sensitivity_labels:
  us_gov: CUI Specified
  uk_gov: OFFICIAL-SENSITIVE
  nz_gov: RESTRICTED
  ca_gov: Protected B
created: '2026-02-08'
updated: '2026-02-08'
author: testpattern-community
license: MIT
