schema: testpattern/v1
name: Password hash databases
slug: global-top500-262-password-hash-databases
version: 1.3.1
type: regex
engine: boost_regex
description: Identifies password hash databases patterns in security and access control contexts. Detects potential exposure of sensitive security information in international systems.
operation: >-
  Pattern type: regex (boost_regex engine). Regex uses word-boundary phrase matching with flexible whitespace. Corroborative evidence: 7 keywords within 300 characters. Keyword lists: credentials.
  Uses additional domain-context evidence and explicit exclusion logic for template/sample/test text.
pattern: (?i)\bpassword\s+hash\s+databases\b
confidence: low
confidence_justification: >-
  Low confidence marker: phrase-based artifact detection to bootstrap line-by-line coverage. Requires corroborative evidence and later hardening to high-confidence structural patterns. Added context
  gating and exclusion rules improve precision and reduce incidental matches.
jurisdictions:
  - global
regulations:
  - GDPR
data_categories:
  - credentials
  - security
corroborative_evidence:
  keywords:
    - password hash databases
    - password
    - hash
    - databases
    - credentials
    - keys
    - secrets
  proximity: 300
  keyword_lists:
    - credentials
    - data-record-context
    - generic-data-labels
test_cases:
  should_match:
    - value: 'Password hash databases'
      description: Topic phrase present
    - value: 'password hash databases'
      description: Topic phrase, lowercase
    - value: 'this record references the password hash databases held on file'
      description: Topic phrase within surrounding text
    - value: 'root:$6$saltsalt$abcdefghijklmnopqrstuvwxyz0123456789ABCDEFhijk:19000:0:99999:7:::'
      description: Unix /etc/shadow SHA-512 hash line for root account (Snaffler KeepNixLocalHashesByName)
  should_not_match:
    - value: unrelated generic text
      description: No relevant phrase context
    - value: placeholder value 12345
      description: Random text should not match phrase marker
    - value: template example placeholder record identifier
      description: Template/sample context should be excluded even when anchor words are present
false_positives:
  - description: Authentication-related terminology in software documentation, security training materials, or system architecture descriptions without actual credentials.
    mitigation: Require proximity to credential-specific patterns (API keys, connection strings, tokens) rather than general security terminology.
  - description: Code snippets and configuration examples containing credential-related keywords or placeholder values in developer documentation.
    mitigation: Check for common placeholder patterns (example.com, localhost, 0000) and documentation file types to reduce false positives from technical writing.
exports:
  - purview_xml
  - yaml
  - regex_copy
scope: wide
purview:
  patterns_proximity: 300
  recommended_confidence: 85
  shared_keywords:
    - dict: template-exclusion
      as: Keyword_noise_exclusion
      match_style: word
    - dict: data-record-context
      as: Evidence_data_record_context
      match_style: word
  pattern_tiers:
    - confidence_level: 85
      id_match: Regex_password_hash_databases
      matches:
        - ref: Pattern_global_top500_262_password_hash_databases_label_context
        - ref: Keyword_global_top500_262_password_hash_databases_domain_context
        - type: any
          min_matches: 0
          max_matches: 0
          refs:
            - Keyword_noise_exclusion
    - confidence_level: 85
      id_match: Regex_password_hash_databases
      matches:
        - ref: Keyword_password_hash_databases
        - type: any
          min_matches: 0
          max_matches: 0
          refs:
            - Keyword_noise_exclusion
    - confidence_level: 65
      discovery_only: true
      id_match: Regex_password_hash_databases
      matches:
        - type: any
          min_matches: 0
          max_matches: 0
          refs:
            - Keyword_noise_exclusion
    - confidence_level: 75
      id_match: Regex_snaffler_shadow_hash_line
      matches:
        - ref: Keyword_snaffler_shadow_context
        - type: any
          min_matches: 0
          max_matches: 0
          refs:
            - Keyword_noise_exclusion
    - confidence_level: 85
      id_match: Regex_snaffler_shadow_hash_line
      matches:
        - ref: Keyword_snaffler_shadow_context
        - ref: Evidence_data_record_context
        - type: any
          min_matches: 0
          max_matches: 0
          refs:
            - Keyword_noise_exclusion
  regexes:
    - id: Regex_password_hash_databases
      pattern: \bpassword\s+hash\s+databases\b
    - id: Pattern_global_top500_262_password_hash_databases_label_context
      pattern: (?i)\b(?:password\s+hash\s+databases)\b
    - id: Regex_snaffler_shadow_hash_line
      pattern: '[A-Za-z0-9_.-]{1,32}:\$(?:1|2[aby]|5|6|y)\$[^\s:]{10,}:'
  keywords:
    - id: Keyword_password_hash_databases
      groups:
        - match_style: word
          terms:
            - password hash
            - hash database
            - bcrypt
            - scrypt
            - argon2
            - sha256
            - md5
            - ntlm
            - credential store
            - shadow file
            - password dump
            - rainbow table
    - id: Keyword_global_top500_262_password_hash_databases_domain_context
      groups:
        - match_style: word
          terms:
            - password hash databases
            - password
            - hash
            - databases
    - id: Keyword_snaffler_shadow_context
      groups:
        - match_style: word
          terms:
            - root
            - daemon
            - nologin
            - shadow
            - passwd
            - /etc/shadow
            - /etc/passwd
sensitivity_labels:
  us_gov: CUI Specified
  uk_gov: OFFICIAL-SENSITIVE
  nz_gov: RESTRICTED
  ca_gov: Protected B
created: '2026-02-08'
updated: '2026-07-09'
author: testpattern-community
license: MIT
references: []
changelog:
  - version: 1.3.1
    date: '2026-07-09'
    description: 'Noise-gate sweep: remove dead shared_keywords import(s) generic-data-labels as Evidence_generic_data_labels — declared but referenced by no tier, so the import only added dead weight to compiled purview.keywords (no behavior change)'
  - version: 1.3.0
    date: '2026-07-08'
    description: >-
      Demoted the ungated bare-phrase tier (Regex_password_hash_databases with only the
      template-exclusion NOT-group) from 75 to 65 discovery_only, matching the sibling top500
      phrase-marker convention (cf. global-top500-261 1.1.0 remediation). The design intent states
      the phrase marker "requires corroborative evidence": gating the 75-tier with credential
      keywords would be tautological (the matched phrase itself contains "password" and "hash", so
      any proximity keyword gate on those terms is auto-satisfied), and an ungated bare topic phrase
      does not carry medium confidence. The gated 75/85 shadow-hash-line tiers and the two
      evidence-gated 85 phrase tiers are unchanged.
  - version: 1.2.0
    date: '2026-07-04'
    description: 'Canonicalized the strongest tier from a non-canonical confidence_level 90 to 85 (only 65/75/85 are valid Purview confidence tiers; 90 was drift, not a deliberate design choice like the AU PSPF marking hierarchy)'
  - version: 1.1.0
    date: '2026-06-26'
    description: >-
      Added Unix /etc/shadow hash-line structure (Snaffler KeepNixLocalHashesByName):
      Regex_snaffler_shadow_hash_line detects username:$algo$hash: lines for MD5,
      bcrypt, SHA-256, SHA-512, and yescrypt algorithms. New tiers at 75 and 85
      gated on shadow/passwd/root/daemon evidence.
