schema: testpattern/v1
name: Asset inventory records
slug: au-top500-307-asset-inventory-records
version: 1.0.0
type: regex
engine: boost_regex
description: >-
  Identifies asset inventory records patterns in security and access control contexts. Detects potential exposure of
  sensitive security information in Australian systems.
operation: >-
  Pattern type: regex (boost_regex engine). Regex uses structural anchors with proximity constraints to match
  domain-specific content. Corroborative evidence: 6 keywords within 300 characters.
pattern: >-
  (?is)\b(backup|disaster\s+recovery|business\s+continuity|failover|capacity|cmdb|endpoint\s+management|dns|maintenance|outage|dependency)\b.{0,220}\b(plan|map|snapshot|catalog|analysis|profile|file)\b
confidence: medium
confidence_justification: category-aware structural regex with anchor and context constraints replaces phrase-only detection.
jurisdictions:
  - au
regulations:
  - general-data-protection
data_categories:
  - credentials
  - security
corroborative_evidence:
  keywords:
    - asset inventory records
    - asset
    - inventory
    - records
    - operations
    - resilience
  proximity: 300
test_cases:
  should_match:
    - value: Asset inventory records
      description: Exact phrase marker match
    - value: asset inventory records
      description: Case-insensitive phrase match
    - value: Asset inventory records
      description: Normalized whitespace phrase
    - value: structured artifact containing anchor terms and contextual fields
      description: Structural anchor sample
  should_not_match:
    - value: unrelated generic text
      description: No relevant phrase context
    - value: placeholder value 12345
      description: Random text should not match phrase marker
    - value: generic prose without structural artifact anchors
      description: Should not match plain mentions
false_positives:
  - description: >-
      Authentication-related terminology in software documentation, security training materials, or system architecture
      descriptions without actual credentials.
    mitigation: >-
      Require proximity to credential-specific patterns (API keys, connection strings, tokens) rather than general
      security terminology.
  - description: >-
      Code snippets and configuration examples containing credential-related keywords or placeholder values in developer
      documentation.
    mitigation: >-
      Check for common placeholder patterns (example.com, localhost, 0000) and documentation file types to reduce false
      positives from technical writing.
exports:
  - purview_xml
  - yaml
  - regex_copy
scope: wide
purview:
  patterns_proximity: 300
  recommended_confidence: 75
  pattern_tiers:
    - confidence_level: 75
      id_match: Regex_asset_inventory
      matches:
        - ref: Keyword_asset_inventory
    - confidence_level: 65
      id_match: Regex_asset_inventory
  regexes:
    - id: Regex_asset_inventory
      pattern: >-
        (?is)\b(backup|disaster\s+recovery|business\s+continuity|failover|capacity|cmdb|endpoint\s+management|dns|maintenance|outage|dependency)\b.{0,220}\b(plan|map|snapshot|catalog|analysis|profile|file)\b
  keywords:
    - id: Keyword_asset_inventory
      groups:
        - match_style: word
          terms:
            - asset inventory
            - asset register
            - hardware inventory
            - software inventory
            - asset tag
            - serial number
            - asset lifecycle
            - IT asset management
            - asset tracking
            - procurement record
            - depreciation schedule
            - asset classification
created: '2026-02-08'
updated: '2026-02-08'
author: testpattern-community
license: MIT
