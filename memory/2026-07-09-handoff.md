# Handoff — 2026-07-09 evening (~21:10 AEST; supersedes the morning checkpoint)

Fresh session on any machine: `/contstart` reads this + the ledger
(`.superpowers/sdd/progress.md` — read today's entries bottom-up for the full trail).

## Where things stand

- **Branch/HEAD:** `main` @ 8b52b5d179, clean tree, synced. Catalog **1,677 patterns**
  (1,655 + 22 Phase-1/2 SITs from the parallel agent), patterns.json current.
- **Concept-strategy: C1 + C3 + C4 COMPLETE** (all merged + KV-published + CF-deployed).
  C2 (discriminator pilot) is the only remaining block — needs a tenant window.
  - C1 (verdict model): patterns PR #22 (pattern_class on all patterns), site PR #8
    (TOPIC_PASS/TOPIC_PARTIAL/TOPIC_FP scoring + vocabulary), site PR #9 + patterns
    PR #24 (re-baseline: 893 verdicts re-labeled from stored rates, 880 raw-cross-checked,
    0 disagreements; sit-design-paper §7 rewritten).
  - C3 (deprecations): patterns PR #26 (17 patterns get status:deprecated +
    deprecation_reason schema, ci-check validated) + site PR #12 (badge/notice).
    Adjudications in `.superpowers/sdd/c3-deprecation-report.md`. Art-9 coverage gap
    (religion/ethnicity/orientation/nationality reference patterns) = D-wave candidate.
  - C4 (xlsx clean-negatives): FOUR doc-gen causes found + fixed (stale metadata masks;
    placeClean table-header no-op; should_not_match embedded in clean negs; filler-corpus
    topic collisions — 298 concept patterns → per-pattern topic filter). Full report:
    `.superpowers/sdd/c4-xlsx-clean-negative-report.md`. Corpus regenerated 3× (final:
    1,655 slugs, 26,480 files, 0 errors; 16-slug verification sweep 0 failures).
    NO pattern-side exclusions added (deliberate — evidence says artefact).
- **Legal 75-tier ticket** (Items 1–2): PR #23 merged; 22 verifier FAILs cleared.
  **NEW verifier baseline: 45 FAILs / 99 warnings** (`--all`).
- **Also merged today:** PR #27 (hotfix: Phase-1 shortAcronyms — see incident below);
  site PRs #10/#11 (Nathan's spun-off chip sessions: _nocache bypass + deploy-path gaps).

## IN-FLIGHT (check first)

1. **Nathan is running the targeted classification RIGHT NOW** (single window, his
   keyboard, started ~21:00, ~6h): 37 batches at
   `Z:\testpattern\test-output\classification-targeted-2026-07-09` then 3 supplement
   batches at `...\classification-supplement-2026-07-09` (the 22 Phase-1/2 SITs).
   Survives this session (his window). `-Resume` continues after any drop.
   **NEXT SESSION'S FIRST JOB when results exist:** fold both result sets via
   `node scripts/generate-compact-results.mjs` (testpattern repo; two-scale-aware, reads
   pattern_class from ../patterns/patterns.json) → refresh patterns repo
   `data/classification-results.json` (PR) → adjudicate residual TOPIC_FP as genuine
   over-match (closes C4's pattern-side question, validates C1 re-baseline).
   NOTE: results land per-OutputDir; generate-compact reads
   test-output/classification/classification-results.json by default — point it at each
   dir (or combine the two dirs' results objects) before folding.
2. **The parallel agent** (Nathan's other session) has been shipping coverage phases
   (PRs #25/#28/#29). **HAZARD: it works in the SAME Z:\patterns checkout** — it switched
   branches under this session once (ledger commit rerouted; recovered by cherry-pick).
   Nathan was asked to move it to its own worktree. Verify `git branch --show-current`
   before every commit.

## Incident log (lessons, also in ledger)

- **KV publish outage 08:49–19:10:** PR #25 merged with RED CI (11 shortAcronyms);
  `check:quality` GATES the publish workflow → every publish failed silently. Fixed by
  hotfix PR #27 (per-term case_sensitive, j2 convention). LESSON: `gh run list` after
  every merge to main; red PR CI = do not merge. (Phase-2 self-fixed via PR #29 — lesson stuck.)
- **Codex misattribution pattern (3× today):** codex review findings referenced code NOT
  in the diff (pre-existing worker/deploy code). Always verify `git diff --name-only
  main...HEAD` covers the finding's file before acting; rebut-and-chip otherwise.

## Machine-local state (Z:\testpattern — NOT in git, /lib/ + generate-*.js gitignored)

The C4 doc-gen fixes live ONLY on the share: `lib/doc-gen/keyword-placer.js` (placeClean
scoped headers/labels, keywordHeading exclusion-aware), `lib/doc-gen/content-builder.js`
(clean-neg zero test values, getCorroborativeKeywords object-term normalize),
`lib/doc-gen/filler-text.js` (setFillerTopicFilter/pickTopicSafe on all template/heading/
title surfaces), `generate-test-docs.js` (filter wiring for clean-neg variants only), and
`test-classification.ps1` (-SharedTenant parallel mode + scoped cleanup; single-window
default path unchanged — Nathan chose single-window tonight). If lost, rebuild from
`.superpowers/sdd/c4-xlsx-clean-negative-report.md`. The doc corpus (dlp_test_docs,
7.4GB) regenerates in ~2.8h via `node generate-test-docs.js`.

## Standing gates (patterns repo)

`npm run check` (0 err / 57 warn) · `npm run check:quality` (PASSED — and it GATES the KV
publish) · `node scripts/verify-pattern-testcases.mjs --all` (**45 FAILs / 99 warnings**)
· `npm run compile` (1,677; patterns.json only as separate chore(build) commit).
Site repo: `npm test` (206/206), `npm run build`.

## Parked / queued

- **case_sensitive 74-file wave** (j2 census) — recommended next pickup, needs Nathan go;
  run as delegated subagent sweep in a C:\claudecode worktree, codex trailing.
- **Full-catalog re-baseline run** (~166 batches ≈ 27h serial / ~9h with -SharedTenant ×3)
  — ~920 previously-tested patterns still carry artefact-polluted clean-neg results;
  schedule with C2's tenant window.
- Smaller: concept test hygiene (e), en-government-classification dict fate, ci-check
  recursive patterns-dir read, marking-collections curation, D2 ticket batch (b)-(l),
  Art-9 reference patterns (D-wave), il-passport (PRADO), format watches.

## Environment (this machine: user `natha`; also see auto-memory)

gh CLI installed but NOT on harness PATH → `"/c/Program Files/GitHub CLI/gh.exe"` (bash);
gh auth via hosts.yml (PAT lacks read:org — warning cosmetic). MSYS mangles `rev:path` →
use PowerShell for `git show ref:path`. Network-share git: stale index.lock after killed
ops → rm + retry; one merge per shell call. Codex reviews from C:\claudecode worktrees
(cannot mount Z:), `-c model_reasoning_effort="high"`, findings on stdout.
