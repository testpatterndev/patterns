# Identifier types

> Generated by `npm run build-identifier-docs` from `data/identifiers/*.yaml`. Do not edit by hand — CI fails if this file is stale.

Classifiers that depend on an organisation-specific or unpublished identifier declare a `customisation` entry of kind `identifier-format`. The public catalogue ships the documented **fallback** regex; a customer deployment replaces or extends it with the real format using the steps below.

| Identifier | Issuer | Public format | Fallback precision | Classifiers |
|---|---|---|---|---|
| [Agency / provider client ID](#agency-client-id) | The customer's service, CRM or provider system | customer-defined | low | 3 |
| [ASIC / MSIC card number](#asic-msic-card-number) | The aviation or maritime security identification card issuing body | unpublished | medium | 1 |
| [Agency case / file / matter number](#case-reference-number) | The customer agency's case management or records system | customer-defined | low | 3 |
| [Crime and Corruption Commission reference](#ccc-reference) | Crime and Corruption Commission (Queensland) | unpublished | medium | 1 |
| [Council property / rate assessment number](#council-property-number) | Queensland local governments | customer-defined | low | 1 |
| [Records management (eDRMS) record number](#edrms-record-number) | The customer's records management system (e.g. Content Manager, RecFind, Objective) | customer-defined | medium | 1 |
| [Employee / staff ID](#employee-id) | The customer's payroll or HR system (e.g. Aurion, SAP, Workday) | customer-defined | low | 2 |
| [Queensland housing client / application number](#housing-client-number) | Queensland Government housing services (Housing Service Centres) | unpublished | low | 2 |
| [Queensland social housing tenancy / property reference](#housing-tenancy-number) | Queensland Government housing services | unpublished | low | 3 |
| [NEM connection agreement number](#nem-connection-agreement-number) | The network service provider (e.g. Powerlink, Energex, Ergon Energy) and connecting generator | customer-defined | medium | 1 |
| [Office of the Independent Assessor complaint reference](#oia-complaint-reference) | Office of the Independent Assessor (Queensland) | unpublished | medium | 1 |
| [QCAT file number](#qcat-file-number) | Queensland Civil and Administrative Tribunal | unpublished | medium | 1 |
| [Queensland Corrective Services IOMS number](#qcs-ioms-number) | Queensland Corrective Services (Integrated Offender Management System) | unpublished | medium | 3 |
| [Queensland corrections order / parole reference](#qcs-order-reference) | Queensland Corrective Services and Parole Board Queensland | unpublished | low | 2 |
| [Queensland court file / proceeding number](#qld-court-file-number) | Queensland courts (Magistrates, District, Supreme) | unpublished | medium | 1 |
| [Education Queensland student ID (EQ ID)](#qld-eq-id) | Queensland Department of Education | partial | medium | 4 |
| [Queensland explosives licence number](#qld-explosives-licence-number) | Resources Safety and Health Queensland (RSHQ), Explosives Inspectorate | unpublished | low | 1 |
| [Queensland explosives security clearance number](#qld-explosives-security-clearance-number) | Resources Safety and Health Queensland (RSHQ), Explosives Inspectorate | unpublished | low | 1 |
| [Queensland Police exhibit / property number](#qps-exhibit-number) | Queensland Police Service | unpublished | medium | 1 |
| [QTAC application number](#qtac-application-number) | Queensland Tertiary Admissions Centre | unpublished | medium | 1 |
| [Regulator prosecution / enforcement reference](#regulator-prosecution-reference) | Queensland regulators (e.g. Workplace Health and Safety Queensland) | unpublished | low | 1 |
| [Surveillance device / warrant reference](#surveillance-warrant-reference) | Queensland law enforcement agencies and issuing courts | unpublished | medium | 1 |
| [University / TAFE student number](#tertiary-student-number) | The customer university or TAFE | customer-defined | low | 4 |
| [Utility customer account number](#utility-account-number) | The customer's energy or water retail billing / CRM system (e.g. SAP IS-U, Gentrack) | customer-defined | low | 2 |
| [Queensland youth justice client / case reference](#youth-justice-reference) | Queensland youth justice services | unpublished | medium | 1 |

## agency-client-id

**Agency / provider client ID** — issued by The customer's service, CRM or provider system.

Client, customer or participant IDs issued by the customer's service system (human services, NDIS providers, councils).

**Appears in:** Client case records; Provider service records; CRM records.

**Labels:** `Client ID`, `Client number`, `Customer number`, `Participant ID`

**Public format:** customer-defined. Entirely customer-specific; no public format exists.

**Public fallback** (precision: low):

```
(?i)\b(?:client|customer|participant)\s+(?:no\.?|number|ID)\s*[:#]?\s*[A-Z]{0,3}\d{4,10}\b
```

Generic client labels are common; the fallback supports other record evidence and the customer format adds precision.

**Customer supplies at engagement:**

- A description of the client ID format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bclient\s+(?:no\.?|number|ID)\s*[:#]?\s*C\d{7}\b
samples: ["Client ID: C4417820", "Client number C0071932", "Client no. C5520117"]
```

**Sensitivity:** Links a person to services they receive; sensitivity follows the service.

**Used by:** `au-ndis-participant-plan-record`, `au-qld-restrictive-practice-behaviour-support`, `au-specialist-homelessness-services-record`

## asic-msic-card-number

**ASIC / MSIC card number** — issued by The aviation or maritime security identification card issuing body.

The unique number recorded for an Aviation Security Identification Card (ASIC) or Maritime Security Identification Card (MSIC) on the issuing body register and card records.

**Appears in:** Register of ASICs (ATSR reg 6.23); Register of MSICs (MTOFSR reg 6.08T); Card holder applications and cancellation records.

**Labels:** `ASIC number`, `MSIC number`, `Card number`, `Card unique number`

**Public format:** unpublished. The regulations require a "unique number" but the card layout is approved privately per issuing body and its details may be protected from disclosure; no public format.

**Public fallback** (precision: medium):

```
(?i)\b(?:ASIC|MSIC|card)\s+(?:unique\s+)?(?:number|no\.?|serial)\s*[:#]\s*(?!(?:n/?a|unknown|tbc|tba|withheld|not\s+recorded)\b|_{2,}|\[|<)[A-Z0-9][A-Z0-9-]{3,19}\b
```

The fallback anchors on the card label; the register structure (holder, card colour, dates, AusCheck outcome) supplies the precision.

**Customer supplies at engagement:**

- A description of the card number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:ASIC|MSIC)\s+(?:number|no\.?)\s*[:#]?\s*[A-Z]{2}\d{7}\b
samples: ["ASIC number: RA0448213", "MSIC number BW0071932", "ASIC no. GR0552011"]
```

**Sensitivity:** Identifies a security-cleared aviation or maritime worker and their access entitlements.

**Used by:** `au-aviation-maritime-security-card-register`

## case-reference-number

**Agency case / file / matter number** — issued by The customer agency's case management or records system.

Internal case, file or matter numbers from the customer's case management system, used across complaint, integrity and service records.

**Appears in:** Integrity and complaint files; Human services case records; Incident and investigation records.

**Labels:** `Case number`, `File number`, `Matter number`, `Reference number`

**Public format:** customer-defined. Entirely customer-specific; no public format exists.

**Public fallback** (precision: low):

```
(?i)\b(?:case|file|matter|reference)\s+(?:no\.?|number|ID|#)\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

Generic case labels are common, so the fallback only supports other evidence; the customer format raises precision substantially.

**Customer supplies at engagement:**

- A description of the case / file number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:case|file)\s+(?:no\.?|number)\s*[:#]?\s*[A-Z]{2}\d{8}\b
samples: ["Case number: HC20264471", "File no. PI20251827", "Case number CR20260412"]
```

**Sensitivity:** Links a person to the customer's case records; sensitivity follows the case type.

**Used by:** `au-ndis-reportable-incident`, `au-qld-public-interest-disclosure`, `au-tertiary-academic-misconduct-record`

## ccc-reference

**Crime and Corruption Commission reference** — issued by Crime and Corruption Commission (Queensland).

Reference numbers used by the CCC and notifying agencies for corrupt conduct notifications, assessments and investigations.

**Appears in:** Section 38 notifications; CCC assessment outcomes and referrals; Agency investigation files.

**Labels:** `CCC reference`, `CCC ref`, `CCC file`

**Public format:** unpublished. The CCC does not publish its reference format.

**Public fallback** (precision: medium):

```
(?i)\bCCC\s+(?:ref(?:erence)?|file|matter)\s*(?:no\.?|number)?\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

The CCC prefix on the label is specific to corruption matters.

**Customer supplies at engagement:**

- A description of the CCC reference format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bCCC\s+ref(?:erence)?\s*[:#]?\s*CO-\d{2}-\d{4}\b
samples: ["CCC reference: CO-26-1187", "CCC ref CO-25-4471", "CCC reference CO-26-0412"]
```

**Sensitivity:** Identifies public officers subject to corruption allegations and the people who reported them.

**Used by:** `au-qld-ccc-corrupt-conduct-notification`

## council-property-number

**Council property / rate assessment number** — issued by Queensland local governments.

Property numbers, rate assessment numbers and similar references used by councils for rating, development and property records.

**Appears in:** Rates notices and concession applications; Closed-meeting rating reports; Development and building records.

**Labels:** `Property number`, `Assessment number`, `Rate assessment number`, `Account number`

**Public format:** customer-defined. Each council numbers properties differently; Brisbane City Council notices show a bill number and account number rather than a published property format.

**Public fallback** (precision: low):

```
(?i)\b(?:property|assessment|rate\s+assessment|rates\s+account)\s+(?:no\.?|number|ID)\s*[:#]?\s*\d{4,12}\b
```

Rating labels are council-specific in wording but not in format; the council supplies the real format.

**Customer supplies at engagement:**

- A description of the property / assessment number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:property|assessment)\s+(?:no\.?|number)\s*[:#]?\s*\d{8}\b
samples: ["Property number: 20441882", "Assessment no. 00719321", "Property number 55201174"]
```

**Sensitivity:** Links a ratepayer to a property and, in concession records, to pension or hardship status.

**Used by:** `au-qld-council-closed-meeting-report`

## edrms-record-number

**Records management (eDRMS) record number** — issued by The customer's records management system (e.g. Content Manager, RecFind, Objective).

Record, document and file numbers assigned by the customer's electronic records management system and printed on letters, briefs, agenda reports and memos.

**Appears in:** Council agenda reports and minutes; Ministerial and executive briefs; Correspondence and memos.

**Labels:** `Our ref`, `Your ref`, `Record number`, `File ref`, `Document number`

**Public format:** customer-defined. Record number formats depend on how the customer configured its eDRMS.

**Public fallback** (precision: medium):

```
(?i)\b(?:our\s+ref|your\s+ref|record\s+(?:no\.?|number)|file\s+ref|doc(?:ument)?\s+(?:no\.?|number|ID))\s*[:#]?\s*[A-Z]{0,4}\d{2,4}[/-]\d{2,8}\b
```

Reference labels plus a year/sequence shape match how most eDRMS numbers are printed without assuming a specific product.

**Customer supplies at engagement:**

- A description of the eDRMS record number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:our\s+ref|record\s+(?:no\.?|number))\s*[:#]?\s*D\d{2}/\d{6}\b
samples: ["Our ref: D26/123456", "Record number D25/004471", "Our ref D26/000412"]
```

**Sensitivity:** Links a document to the customer's records; sensitivity follows the record.

**Used by:** `au-qld-council-closed-meeting-report`

## employee-id

**Employee / staff ID** — issued by The customer's payroll or HR system (e.g. Aurion, SAP, Workday).

Staff numbers issued by the customer's payroll or HR system, used on HR, integrity and disclosure records.

**Appears in:** Integrity and disclosure records; HR case files; Payroll and leave records.

**Labels:** `Employee number`, `Employee ID`, `Staff number`, `Payroll number`

**Public format:** customer-defined. Entirely customer-specific.

**Public fallback** (precision: low):

```
(?i)\b(?:employee|staff|payroll)\s+(?:no\.?|number|ID)\s*[:#]?\s*[A-Z]{0,3}\d{4,10}\b
```

Employee labels are common in HR documents; the fallback supports other evidence.

**Customer supplies at engagement:**

- A description of the employee ID format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:employee|staff)\s+(?:no\.?|number|ID)\s*[:#]?\s*\d{8}\b
samples: ["Employee ID: 00447182", "Staff number 00552011", "Employee no. 00071932"]
```

**Sensitivity:** Links an employee to integrity, disclosure or HR matters.

**Used by:** `au-qld-ccc-corrupt-conduct-notification`, `au-qld-public-interest-disclosure`

## housing-client-number

**Queensland housing client / application number** — issued by Queensland Government housing services (Housing Service Centres).

Client and application numbers used for social housing applications, housing register records and private rental assistance.

**Appears in:** Application for Social Housing; Housing register and needs assessments; Bond Loan and Rental Grant applications.

**Labels:** `Application number`, `Application No.`, `Client number`, `Customer number`

**Public format:** unpublished. The older housing application form shows an "Application No." field, but the number format is not published.

**Public fallback** (precision: low):

```
(?i)\b(?:application|client|customer)\s+(?:no\.?|number|ID)\s*[:#]?\s*[A-Z]{0,3}\d{5,10}\b
```

Anchored on the labels printed on housing forms; generic enough that it relies on the housing primary for context.

**Customer supplies at engagement:**

- A description of the housing client / application number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:application|client)\s+(?:no\.?|number)\s*[:#]?\s*\d{7}\b
samples: ["Application number: 4471820", "Client no. 0071932", "Application No. 5520117"]
```

**Sensitivity:** Links a household to housing need, income and often domestic violence or disability circumstances.

**Used by:** `au-qld-public-housing-tenancy-breach`, `au-qld-social-housing-application`

## housing-tenancy-number

**Queensland social housing tenancy / property reference** — issued by Queensland Government housing services.

Tenancy and property references for social housing tenancies and rental properties.

**Appears in:** Tenancy management and breach records; Tenancy exit and bond records; Property and maintenance records.

**Labels:** `Tenancy number`, `Tenancy ID`, `Property ID`, `Lease number`

**Public format:** unpublished. Tenancy and property reference formats are not published. RTA bond numbers are documented separately as up to 9 digits.

**Public fallback** (precision: low):

```
(?i)\b(?:tenancy|tenant|lease|property)\s+(?:no\.?|number|ID)\s*[:#]?\s*[A-Z]{0,3}\d{5,10}\b
```

Tenancy labels are fairly specific to housing records; the value shape stays loose.

**Customer supplies at engagement:**

- A description of the tenancy / property reference format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\btenancy\s+(?:no\.?|number|ID)\s*[:#]?\s*T\d{8}\b
samples: ["Tenancy number: T20417733", "Tenancy ID T00719321", "Tenancy no. T55201174"]
```

**Sensitivity:** Links a household to an address and to tenancy enforcement history.

**Used by:** `au-qld-dfv-tenancy-exit-notice`, `au-qld-public-housing-tenancy-breach`, `au-qld-social-housing-application`

## nem-connection-agreement-number

**NEM connection agreement number** — issued by The network service provider (e.g. Powerlink, Energex, Ergon Energy) and connecting generator.

Reference numbers for generator and large-load connection agreements between a network service provider and a connecting party in the National Electricity Market, printed on performance standards, data sheets and connection correspondence.

**Appears in:** Generator performance standards; Power system design and setting data sheets; Connection agreements and correspondence.

**Labels:** `Connection agreement number`, `Connection agreement no.`, `Connection agreement reference`

**Public format:** customer-defined. Set by each network service provider; no public format.

**Public fallback** (precision: medium):

```
(?i)\bconnection\s+agreement\s+(?:no\.?|number|ref(?:erence)?)\s*[:#]\s*(?!(?:n/?a|unknown|tbc|tba|withheld|not\s+recorded)\b|_{2,}|\[|<)[A-Z0-9][A-Z0-9/-]{3,24}\b
```

The label is specific to NEM connections; plant fields and model parameters supply further precision.

**Customer supplies at engagement:**

- A description of the connection agreement number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bconnection\s+agreement\s+(?:no\.?|number)\s*[:#]?\s*PL-GEN-\d{4}\b
samples: ["Connection agreement number: PL-GEN-0448", "connection agreement no. PL-GEN-2047", "Connection Agreement Number PL-GEN-0071"]
```

**Sensitivity:** Identifies a generating system connection and its confidential technical and commercial terms.

**Used by:** `au-aemo-generator-performance-standards-model`

## oia-complaint-reference

**Office of the Independent Assessor complaint reference** — issued by Office of the Independent Assessor (Queensland).

Reference numbers for councillor conduct complaints assessed or investigated by the Office of the Independent Assessor.

**Appears in:** Councillor conduct complaints and assessments; Natural justice letters; Referrals to councils or the Councillor Conduct Tribunal.

**Labels:** `OIA reference`, `OIA ref`, `OIA file`

**Public format:** unpublished. The OIA does not publish its reference format.

**Public fallback** (precision: medium):

```
(?i)\bOIA\s+(?:ref(?:erence)?|file|case)\s*(?:no\.?|number)?\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

The OIA prefix is specific to councillor conduct matters in Queensland.

**Customer supplies at engagement:**

- A description of the OIA reference format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bOIA\s+ref(?:erence)?\s*[:#]?\s*OIA-\d{2}-\d{5}\b
samples: ["OIA reference: OIA-26-00412", "OIA ref OIA-25-11827", "OIA reference OIA-26-04471"]
```

**Sensitivity:** Links a named councillor and complainant to conduct allegations.

**Used by:** `au-qld-councillor-conduct-complaint`

## qcat-file-number

**QCAT file number** — issued by Queensland Civil and Administrative Tribunal.

File number assigned by QCAT to applications, including guardianship and administration matters and tenancy disputes.

**Appears in:** QCAT applications (e.g. Form 10 guardianship and administration); Interim and final orders; Tenancy dispute applications.

**Labels:** `QCAT file number`, `QCAT reference`, `File number`

**Public format:** unpublished. QCAT does not publish its file number format.

**Public fallback** (precision: medium):

```
(?i)\bQCAT\s+(?:file\s+)?(?:no\.?|number|ref(?:erence)?)\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

The QCAT prefix on the label is specific; the value format is left loose because it is unpublished.

**Customer supplies at engagement:**

- A description of the QCAT file number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bQCAT\s+(?:file\s+)?(?:no\.?|number|ref(?:erence)?)\s*[:#]?\s*[A-Z]{3}\d{4,6}-\d{2}\b
samples: ["QCAT file number: GAA4471-26", "QCAT ref OCR11827-25", "QCAT number GAA0412-26"]
```

**Sensitivity:** Guardianship matters concern adults with impaired capacity; tenancy matters link people to addresses and disputes.

**Used by:** `au-qld-qcat-guardianship-administration`

## qcs-ioms-number

**Queensland Corrective Services IOMS number** — issued by Queensland Corrective Services (Integrated Offender Management System).

Identifier for a prisoner or offender in QCS's Integrated Offender Management System (IOMS), used on custodial orders, parole, community corrections and Victims Register records. The public "Contact a prisoner" and prisoner search pages ask for the IOMS number.

**Appears in:** QCS statutory orders (safety, maximum security, separate confinement, transfer); Parole Board Queensland decisions and parole orders; Victims Register records; Prisoner correspondence and visits.

**Labels:** `IOMS number`, `IOMS No.`, `IOMS`

**Public format:** unpublished. QCS publishes the label "IOMS number" but not the value format. The older Correctional Information System (CIS) number may appear in legacy records.

**Public fallback** (precision: medium):

```
(?i)\bIOMS\s*(?:no\.?|number|#)?\s*[:#]?\s*[A-Z0-9][A-Z0-9-]{3,15}\b
```

The "IOMS" label is specific to Queensland corrections, so a labelled value is a strong record signal even without the value format; unlabelled IOMS numbers are not detected until the customer format is supplied.

**Customer supplies at engagement:**

- A description of the IOMS number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bIOMS\s*(?:no\.?|number)?\s*[:#]?\s*\d{7}\b
samples: ["IOMS number: 4471820", "IOMS 5520117", "IOMS No. 0071932"]
```

**Sensitivity:** Identifies a person as a prisoner or offender; combined with a victim record it links victim and offender.

**Used by:** `au-qld-corrections-victims-register`, `au-qld-corrective-services-statutory-order`, `au-qld-parole-board-decision`

## qcs-order-reference

**Queensland corrections order / parole reference** — issued by Queensland Corrective Services and Parole Board Queensland.

Reference numbers on community corrections orders, parole orders and Parole Board Queensland decisions (amendment, suspension, cancellation).

**Appears in:** Community corrections orders; Court ordered and board ordered parole orders; Parole Board Queensland decisions.

**Labels:** `Order number`, `Parole order number`, `PBQ reference`, `Reference`

**Public format:** unpublished. No public format. The previous catalogue used an invented CCO-/PB-YYYY- format, which real documents do not contain.

**Public fallback** (precision: low):

```
(?i)\b(?:parole\s+order|community\s+(?:corrections|service)\s+order|PBQ|order)\s+(?:no\.?|number|ref(?:erence)?)\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

Anchored on the order-number labels used on corrections documents; the value shape is kept loose because no format is published.

**Customer supplies at engagement:**

- A description of the order / parole reference format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:PBQ|parole\s+order)\s+(?:no\.?|number|ref(?:erence)?)\s*[:#]?\s*\d{4}-\d{6}\b
samples: ["PBQ reference: 2026-004471", "Parole order number 2025-118273", "PBQ ref 2026-000412"]
```

**Sensitivity:** Links a named person to a custodial or community supervision decision.

**Used by:** `au-qld-parole-board-decision`, `community-corrections-order`

## qld-court-file-number

**Queensland court file / proceeding number** — issued by Queensland courts (Magistrates, District, Supreme).

File or proceeding numbers assigned by Queensland court registries to civil and criminal proceedings.

**Appears in:** Court filings and briefs; Orders and judgments; Registry correspondence.

**Labels:** `Court file number`, `File number`, `Proceeding number`, `Cause number`, `Registry number`

**Public format:** unpublished. Registry number formats are not published in a checkable form. The previous catalogue used an invented MAG-/DC-/SC-YYYY- format.

**Public fallback** (precision: medium):

```
(?i)\b(?:court\s+file|proceeding|cause|registry)\s+(?:no\.?|number)\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

Court-specific labels make a labelled value a reasonable record signal without guessing the registry format.

**Customer supplies at engagement:**

- A description of the court file number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:court\s+file|proceeding)\s+(?:no\.?|number)\s*[:#]?\s*[A-Z]{2,3}\s?\d{3,5}/\d{2}\b
samples: ["Court file number: BS 4471/26", "Proceeding number MAG 11827/25", "Court file no. DC 412/26"]
```

**Sensitivity:** Links a named party to court proceedings, which may be sealed or subject to publication restrictions.

**Used by:** `court-filing-reference`

## qld-eq-id

**Education Queensland student ID (EQ ID)** — issued by Queensland Department of Education.

State school student identifier shown on report cards, invoices, student ID cards and OneSchool records.

**Appears in:** Report cards and invoices; OneSchool records and plans; Suspension, exclusion and adjustment records.

**Labels:** `EQ ID`, `EQID`, `Student ID`

**Public format:** partial — `\d{10}[A-Z]` ([source](https://tewantinss.eq.edu.au/support-and-resources/qparents)). A school QParents page describes the EQ ID as "10 numbers and one letter" but does not say where the letter sits; no Department of Education specification was found.

**Public fallback** (precision: medium):

```
(?i)\bEQ\s*ID\s*[:#]?\s*[A-Z0-9]{10,12}\b
```

The EQ ID label is specific to Queensland state schools; the value allows 10-12 alphanumerics until the letter position is confirmed from a real sample.

**Customer supplies at engagement:**

- A description of the EQ ID format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bEQ\s*ID\s*[:#]?\s*\d{10}[A-Z]\b
samples: ["EQ ID: 0048213772K", "EQID 0071932845L", "EQ ID 1122334455M"]
```

**Sensitivity:** Identifies a school student (a minor); appears on behaviour, disability and discipline records.

**Used by:** `au-education-nccd-adjustment-record`, `au-qcaa-aara-illness-misadventure`, `au-qld-oneschool-student-support-plan`, `au-qld-school-suspension-exclusion-decision`

## qld-explosives-licence-number

**Queensland explosives licence number** — issued by Resources Safety and Health Queensland (RSHQ), Explosives Inspectorate.

Licence numbers for explosives authorities in Queensland, including licences to use or store security sensitive ammonium nitrate (SSAN), recorded on security plans, access lists and receipts.

**Appears in:** SSAN security plans; Explosives safety and security management systems; Receipts and chain-of-possession records.

**Labels:** `Licence number`, `Licence no.`, `Explosives licence`

**Public format:** unpublished. The chief inspector keeps a public register of authorities, but the licence number format is not published.

**Public fallback** (precision: low):

```
(?i)\b(?:explosives\s+)?licen[cs]e\s+(?:no\.?|number)\s*[:#]\s*(?!(?:n/?a|unknown|tbc|tba|withheld|not\s+recorded)\b|_{2,}|\[|<)[A-Z0-9][A-Z0-9/-]{3,19}\b
```

Licence labels are generic; the SSAN security-plan structure and UN numbers supply the precision.

**Customer supplies at engagement:**

- A description of the licence number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\blicen[cs]e\s+(?:no\.?|number)\s*[:#]?\s*EX\d{7}\b
samples: ["Licence number: EX0448213", "licence no. EX2047181", "Licence Number EX0071932"]
```

**Sensitivity:** Links a person or site to the storage or use of security sensitive explosives.

**Used by:** `au-qld-ssan-security-plan`

## qld-explosives-security-clearance-number

**Queensland explosives security clearance number** — issued by Resources Safety and Health Queensland (RSHQ), Explosives Inspectorate.

Security clearance numbers held by people with unsupervised access to security sensitive explosives in Queensland, listed in SSAN security plans and access registers.

**Appears in:** SSAN security plan access lists; Unsupervised access registers.

**Labels:** `Clearance number`, `Security clearance number`, `Clearance holder`

**Public format:** unpublished. Clearances are required for unsupervised access (Explosives Regulation 2017 ss 18B-18C); the number format is not published.

**Public fallback** (precision: low):

```
(?i)\b(?:security\s+)?clearance\s+(?:no\.?|number|holder)\s*[:#]\s*(?!(?:n/?a|unknown|tbc|tba|withheld|not\s+recorded)\b|_{2,}|\[|<)[A-Z0-9][A-Z0-9/-]{3,19}\b
```

Clearance labels also appear in government security vetting; the SSAN plan structure supplies the precision.

**Customer supplies at engagement:**

- A description of the clearance number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bclearance\s+(?:no\.?|number)\s*[:#]?\s*SC\d{7}\b
samples: ["Clearance number: SC0448213", "clearance no. SC2047181", "Security clearance number SC0071932"]
```

**Sensitivity:** Identifies people authorised for unsupervised access to security sensitive explosives.

**Used by:** `au-qld-ssan-security-plan`

## qps-exhibit-number

**Queensland Police exhibit / property number** — issued by Queensland Police Service.

Number assigned to an exhibit or seized property item, used on chain-of-custody records, evidence handling logs and forensic submissions.

**Appears in:** Chain-of-custody records; Evidence and property handling logs; Forensic submission forms.

**Labels:** `Exhibit number`, `Exhibit No.`, `Property tag`, `Property number`, `Evidence bag number`, `Seal number`

**Public format:** unpublished. QPS does not publish its exhibit, property, evidence bag or seal numbering. The previous catalogue used invented QPS-EX-YYYY- and EV-NNNN formats.

**Public fallback** (precision: medium):

```
(?i)\b(?:exhibit|property\s+tag|property|evidence\s+bag|seal)\s+(?:no\.?|number|ID|#)\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

Exhibit and property-tag labels are specific to evidence handling; together with the chain-of-custody primary they indicate a genuine record.

**Customer supplies at engagement:**

- A description of the exhibit / property number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:exhibit|property\s+tag)\s+(?:no\.?|number|#)?\s*[:#]?\s*\d{2}[A-Z]{2}\d{6}\b
samples: ["Exhibit number: 26EX004471", "Property tag 26PT118273", "Exhibit # 25EX000412"]
```

**Sensitivity:** Links evidence to an active investigation; disclosure can compromise prosecutions.

**Used by:** `forensic-evidence-chain-of-custody-active`

## qtac-application-number

**QTAC application number** — issued by Queensland Tertiary Admissions Centre.

Application number issued by QTAC to tertiary applicants, printed on EAS forms, offers and correspondence.

**Appears in:** Educational Access Scheme forms and statements; Offers and QTAC correspondence.

**Labels:** `QTAC application number`, `Application number`

**Public format:** unpublished. QTAC does not publish the application number format.

**Public fallback** (precision: medium):

```
(?i)\bQTAC\s+application\s+(?:no\.?|number)\s*[:#]?\s*\d{5,10}\b
```

The QTAC prefix on the label is specific; the numeric length range is kept wide because the format is unpublished.

**Customer supplies at engagement:**

- A description of the QTAC application number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bQTAC\s+application\s+(?:no\.?|number)\s*[:#]?\s*\d{7}\b
samples: ["QTAC application number: 2641187", "QTAC application no. 2649921", "QTAC application number 2600412"]
```

**Sensitivity:** Links an applicant to hardship, illness and disability statements.

**Used by:** `au-qtac-educational-access-scheme`

## regulator-prosecution-reference

**Regulator prosecution / enforcement reference** — issued by Queensland regulators (e.g. Workplace Health and Safety Queensland).

File or reference numbers used by a regulator for prosecution briefs, enforcement actions and investigations.

**Appears in:** Regulatory prosecution briefs; Enforcement notices and investigation files.

**Labels:** `Prosecution reference`, `Enforcement reference`, `Investigation number`, `File number`

**Public format:** unpublished. Formats are regulator-specific and unpublished. The previous catalogue used invented WHS-PROS- and ENF- formats.

**Public fallback** (precision: low):

```
(?i)\b(?:prosecution|enforcement|investigation|compliance)\s+(?:ref(?:erence)?|no\.?|number|file)\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

Anchored on the reference labels regulators use; the value shape is loose because formats differ between regulators.

**Customer supplies at engagement:**

- A description of the prosecution / enforcement reference format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:prosecution|investigation)\s+(?:ref(?:erence)?|no\.?|number)\s*[:#]?\s*[A-Z]{3}\d{6}\b
samples: ["Prosecution reference: WHS004471", "Investigation no. INV118273", "Prosecution number WHS000412"]
```

**Sensitivity:** Identifies the subject of a regulatory prosecution before court outcomes are public.

**Used by:** `regulatory-prosecution-brief`

## surveillance-warrant-reference

**Surveillance device / warrant reference** — issued by Queensland law enforcement agencies and issuing courts.

Reference numbers for surveillance device warrants and related authorisations referenced in surveillance operation plans.

**Appears in:** Surveillance operation plans; Surveillance device warrant applications and reports.

**Labels:** `Warrant number`, `Warrant reference`, `Surveillance device warrant`, `Authorisation number`

**Public format:** unpublished. No public format. The previous catalogue used an invented TSD-/LD-YYYY- format.

**Public fallback** (precision: medium):

```
(?i)\b(?:surveillance\s+device\s+warrant|warrant|authorisation)\s+(?:no\.?|number|ref(?:erence)?)\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

Warrant-number labels inside an operation plan are a strong signal; value formats vary by issuing court and agency.

**Customer supplies at engagement:**

- A description of the warrant reference format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bwarrant\s+(?:no\.?|number|ref(?:erence)?)\s*[:#]?\s*SDW\d{7}\b
samples: ["Warrant number: SDW2604471", "Warrant ref SDW2511827", "Warrant no. SDW2600412"]
```

**Sensitivity:** Reveals covert investigative activity; disclosure can endanger operations and people.

**Used by:** `surveillance-operation-plan`

## tertiary-student-number

**University / TAFE student number** — issued by The customer university or TAFE.

Student numbers issued by a university or TAFE, printed on access plans, misconduct, progression and special consideration records.

**Appears in:** Disability access plans; Academic misconduct and progression records; Special consideration applications.

**Labels:** `Student number`, `Student ID`, `Student No.`

**Public format:** customer-defined. Each institution has its own format (some prefix a letter, e.g. s or n); none is published as a national standard.

**Public fallback** (precision: low):

```
(?i)\bstudent\s+(?:no\.?|number|ID)\s*[:#]?\s*[A-Za-z]?\d{6,9}\b
```

Student-number labels are common across education documents; the institution format raises precision.

**Customer supplies at engagement:**

- A description of the student number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bstudent\s+(?:no\.?|number|ID)\s*[:#]?\s*n\d{8}\b
samples: ["Student number: n11234567", "Student ID n10071932", "Student no. n09552011"]
```

**Sensitivity:** Links a student to disability, integrity or hardship records.

**Used by:** `au-tertiary-academic-misconduct-record`, `au-tertiary-academic-progression-show-cause`, `au-tertiary-disability-access-plan`, `au-tertiary-special-consideration-application`

## utility-account-number

**Utility customer account number** — issued by The customer's energy or water retail billing / CRM system (e.g. SAP IS-U, Gentrack).

Customer and account numbers issued by an energy retailer, water utility or council billing system, printed on bills, hardship, life support and family violence customer records.

**Appears in:** Energy and water bills; Life support registers; Hardship and family violence customer records; Collections and disconnection records.

**Labels:** `Account number`, `Customer number`, `Customer ID`, `Account ID`

**Public format:** customer-defined. Retailer- and utility-specific; no public format. NMIs and MIRNs are separate, public identifiers.

**Public fallback** (precision: low):

```
(?i)\b(?:customer|account)\s+(?:no\.?|number|ID)\s*[:#]?\s*(?!(?:n/?a|unknown|tbc|tba|withheld|not\s+recorded)\b|_{2,}|\[|<)[A-Z]{0,3}\d{5,12}\b
```

Account and customer labels are common in business records; the fallback only supports utility-specific evidence (NMI, supply address, life support or family violence terms).

**Customer supplies at engagement:**

- A description of the customer / account number format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\b(?:customer|account)\s+(?:no\.?|number)\s*[:#]?\s*40\d{8}\b
samples: ["Account number: 4001882731", "Customer number 4000552011", "account no. 4007193245"]
```

**Sensitivity:** Links a named customer, supply address and sensitive circumstances (medical equipment, family violence, hardship) to a utility account.

**Used by:** `au-energy-family-violence-customer-record`, `au-energy-life-support-register`

## youth-justice-reference

**Queensland youth justice client / case reference** — issued by Queensland youth justice services.

Client or case reference used by Queensland youth justice services for a young person on orders, in detention or on remand.

**Appears in:** Youth justice orders and conditional release orders; Youth detention centre admissions; Restorative justice agreements.

**Labels:** `Client number`, `YJ reference`, `Youth justice reference`, `Case number`

**Public format:** unpublished. No public format was found for youth justice client or case references.

**Public fallback** (precision: medium):

```
(?i)\b(?:youth\s+justice|YJ)\s+(?:client\s+)?(?:no\.?|number|ID|ref(?:erence)?)\s*[:#]?\s*[A-Z0-9][A-Z0-9/-]{3,20}\b
```

The youth-justice-specific label keeps precision reasonable; generic "client number" labels are left to the record-structure evidence.

**Customer supplies at engagement:**

- A description of the youth justice reference format as issued by the customer's system (length, prefixes, separators, check digit if any).
- The label text used on documents and exports (for example the exact field heading).
- 5-10 masked or fictional sample values in the real format (never live values in the engagement record).
- Whether the value always appears next to its label, or can also appear on its own (e.g. in spreadsheets).

**How to replace:**

1. Collect the customer inputs listed above during the engagement discovery session.
2. Write a Boost-compatible regex for the value, anchored with \b and bounded quantifiers only (no nested or unbounded group quantifiers).
3. If the identifier is only reliable next to its label, keep the documented label prefix in the regex.
4. Record the regex and samples in the deployment overlay under this identifier key; the package builder applies it to every classifier slot that references this identifier (extend adds an alternative, replace swaps the regex).
5. Re-run the classifier test cases with the overlay applied and check at least one real (redacted) document before enabling enforcement.

**Overlay must pass:**

- Every supplied sample matches the overlay regex; none of the classifier's should_not_match values do.
- The regex passes the Purview-banned construct checks (scripts/lib/purview-banned.mjs) and compiles in Boost and JavaScript.
- The regex does not match all-same-digit or obviously placeholder values (000000, 123456, XXXXXX).
- Overlay values never appear in build logs or readiness output (counts and keys only).

**Example overlay (fictional):**

```yaml
pattern: (?i)\bYJ\s*(?:ref(?:erence)?|client)?\s*[:#]?\s*\d{8}\b
samples: ["YJ reference: 44718201", "YJ 55201174", "YJ client 00719321"]
```

**Sensitivity:** Identifies a child in the youth justice system; subject to strong confidentiality provisions.

**Used by:** `au-qld-youth-justice-order-record`
