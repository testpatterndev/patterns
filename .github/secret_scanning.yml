# TestPattern is an open registry of DLP detection patterns. Pattern files and the
# compiled artifact necessarily contain SYNTHETIC example tokens that match real
# credential formats by construction (that is what the patterns detect). All test
# values are zero-entropy filler per CONTRIBUTING conventions; real secrets are
# still scanned everywhere else in the repo.
paths-ignore:
  - "data/patterns/**"
  - "patterns.json"
